let userService;_c01‍.x([["default",()=>_c01‍.o]]);_c01‍.w("../Service/UserService",[["default",["userService"],function(v){userService=v}]]);

class UserController {
    async postUser(req, res) {
        try {
            const { firstName, lastName, email, password } = req.body;
            const data = await userService.postUser(firstName, lastName, email, password);

            res.status(200).send(data);
        } catch (error) {
            res.status(400).send(error);
        }
    }

    async login(req, res){
        try {
            const { email, password }= req.body;
            const data = await userService.login(email, password);

            res.status(200).send(data);
        } catch (error) {
            res.status(400).send(error);
        }
    }
}

_c01‍.d(new UserController());
